<!DOCTYPE html>
<html lang="pt-BR" data-theme="dark">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<title>Foto → Emoji - Detilli Hub</title>
<meta name="theme-color" content="#0a0f1f">
<style>
/* ═══════════════════════════════════════════
   VARIÁVEIS DE TEMA — ESCURO (padrão) / CLARO
   ═══════════════════════════════════════════ */
:root {
  --bg-page:     #02020a;
  --bg-surface:  rgba(255,255,255,.09);
  --bg-solid:    #0f172a;
  --border:      rgba(148,163,184,.18);
  --text:        #e6edf3;
  --text-2:      #94a3b8;
  --text-3:      #64748b;
  --accent:      #29b6f6;
  --accent-2:    #0284c7;
  --accent-glow: rgba(41,182,246,.35);
  --btn-bg:      rgba(255,255,255,.08);
  --btn-color:   #e5e7eb;
  --btn-border:  rgba(255,255,255,.14);
  --in-bg:       rgba(2,6,23,.75);
  --in-border:   rgba(148,163,184,.28);
  --in-color:    #e5e7eb;
  --chip-bg:     rgba(255,255,255,.06);
  --chip-border: rgba(255,255,255,.14);
  --chip-color:  #cbd5e1;
  --pill-on-bg:  linear-gradient(180deg,rgba(20,184,166,.28),rgba(139,92,246,.28));
  --pill-on-bdr: rgba(167,139,250,.7);
  --sep:         rgba(148,163,184,.2);
  --track:       linear-gradient(90deg,rgba(20,184,166,.5),rgba(139,92,246,.5));
  --thumb-color: #7c3aed;
  --cvs-bg:      repeating-conic-gradient(from 0deg,#0b1220 0 25%,#0a0f1c 0 50%) 0 0/22px 22px;
  --glow-op:     .5;
}
[data-theme="light"] {
  --bg-page:     #f0f4f8;
  --bg-surface:  #ffffff;
  --bg-solid:    #ffffff;
  --border:      #e2e8f0;
  --text:        #0f172a;
  --text-2:      #475569;
  --text-3:      #94a3b8;
  --accent:      #2563eb;
  --accent-2:    #1d4ed8;
  --accent-glow: rgba(37,99,235,.2);
  --btn-bg:      #f1f5f9;
  --btn-color:   #334155;
  --btn-border:  #e2e8f0;
  --in-bg:       #ffffff;
  --in-border:   #cbd5e1;
  --in-color:    #0f172a;
  --chip-bg:     #f1f5f9;
  --chip-border: #e2e8f0;
  --chip-color:  #334155;
  --pill-on-bg:  linear-gradient(180deg,rgba(37,99,235,.12),rgba(99,102,241,.12));
  --pill-on-bdr: #3b82f6;
  --sep:         #e2e8f0;
  --track:       linear-gradient(90deg,#3b82f6,#6366f1);
  --thumb-color: #2563eb;
  --cvs-bg:      repeating-conic-gradient(from 0deg,#e2e8f0 0 25%,#f8fafc 0 50%) 0 0/22px 22px;
  --glow-op:     .08;
}

/* ═══════ BASE ═══════ */
*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  font-family: ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,"Helvetica Neue",Arial,
               "Noto Color Emoji","Apple Color Emoji","Segoe UI Emoji",sans-serif;
  color:var(--text);
  background:var(--bg-page);
  -webkit-font-smoothing:antialiased;
  overflow-x:hidden;
  transition:background .25s,color .25s;
}

/* ── BACKGROUNDS ── */
.bg{
  position:fixed;inset:0;z-index:-2;
  background:
    radial-gradient(1100px 700px at 10% -5%,rgba(20,184,166,.35),transparent 60%),
    radial-gradient(900px 600px at 100% 0%,rgba(139,92,246,.38),transparent 55%),
    radial-gradient(700px 500px at 50% 120%,rgba(14,165,233,.25),transparent 50%),
    linear-gradient(180deg,#030712 0%,#02020a 100%);
  transition:opacity .3s;
}
[data-theme="light"] .bg{opacity:.25}
.glow{
  position:fixed;inset:-20%;z-index:-1;filter:blur(60px);pointer-events:none;
  opacity:var(--glow-op);
  background:conic-gradient(from 180deg at 50% 50%,rgba(20,184,166,.2),rgba(139,92,246,.2),rgba(6,182,212,.2),rgba(20,184,166,.2));
  animation:spin 24s linear infinite;transition:opacity .3s;
}
@keyframes spin{to{transform:rotate(360deg)}}

/* ── LAYOUT ── */
.wrap{max-width:1200px;margin:0 auto;padding:0 16px 56px}
header{
  display:flex;align-items:center;justify-content:space-between;gap:12px;
  padding:16px 0 20px;flex-wrap:wrap;
}
.brand{display:flex;align-items:center;gap:12px}
.logo{
  width:44px;height:44px;border-radius:14px;display:grid;place-items:center;font-weight:900;
  color:#03131a;letter-spacing:.5px;
  background:conic-gradient(from 210deg,#14b8a6,#8b5cf6,#06b6d4,#14b8a6);
  box-shadow:0 8px 30px rgba(139,92,246,.35),inset 0 0 16px rgba(255,255,255,.25),inset 0 -8px 20px rgba(0,0,0,.25);
  flex-shrink:0;
}
h1{margin:0;font-size:22px;line-height:1.1;color:var(--text)}
.sub{font-size:12px;color:var(--accent);margin-top:2px;letter-spacing:.3px}

/* ─── SELETOR DE TEMA ─── */
.theme-toggle{
  display:flex;align-items:center;background:var(--bg-surface);
  border:1px solid var(--border);border-radius:14px;padding:4px;gap:3px;
  backdrop-filter:blur(14px);
}
.theme-btn{
  appearance:none;border:0;border-radius:10px;padding:7px 11px;
  font-size:12px;font-weight:700;cursor:pointer;
  background:transparent;color:var(--text-2);
  transition:background .18s,color .18s,box-shadow .18s;display:flex;align-items:center;gap:5px;
}
.theme-btn.active{
  background:var(--accent);color:#fff;
  box-shadow:0 4px 12px var(--accent-glow);
}

main{display:grid;grid-template-columns:380px 1fr;gap:18px;align-items:start}
@media(max-width:920px){main{grid-template-columns:1fr}}

/* ── CARD ── */
.card{
  background:var(--bg-surface);
  border:1px solid var(--border);
  border-radius:22px;
  box-shadow:0 20px 60px rgba(0,0,0,.3),inset 0 1px 0 rgba(255,255,255,.05);
  backdrop-filter:blur(14px);-webkit-backdrop-filter:blur(14px);
  margin-bottom:14px;
  transition:background .25s,border-color .25s;
}
[data-theme="light"] .card{box-shadow:0 4px 24px rgba(0,0,0,.08),0 1px 3px rgba(0,0,0,.06)}
.pad{padding:18px}
.card h2{margin:0 0 12px;font-size:13px;letter-spacing:.4px;text-transform:uppercase;color:var(--accent);font-weight:800}

/* ── UTILITÁRIOS ── */
.stack{display:flex;flex-direction:column;gap:10px}
.row{display:flex;gap:10px;align-items:center}
.row.wrap{flex-wrap:wrap}
.sep{height:1px;margin:14px 0;background:var(--sep)}
.note{font-size:12px;color:var(--text-3)}

/* ── BOTÕES ── */
.btn{
  appearance:none;border:1px solid var(--btn-border);border-radius:14px;padding:12px 14px;font-weight:800;letter-spacing:.2px;
  background:var(--btn-bg);color:var(--btn-color);cursor:pointer;display:inline-flex;align-items:center;justify-content:center;gap:8px;
  transition:transform .08s,filter .15s,opacity .15s;white-space:nowrap;user-select:none;font-size:14px;
}
.btn:hover{filter:brightness(1.08)}
.btn:active{transform:translateY(1px) scale(.99)}
.btn:disabled{opacity:.45;cursor:not-allowed}
.btn-primary{
  background:linear-gradient(90deg,#14b8a6,#7c3aed 60%,#8b5cf6);
  color:#fff;border:0;box-shadow:0 12px 30px rgba(124,58,237,.35),inset 0 1px 0 rgba(255,255,255,.15);
}
[data-theme="light"] .btn-primary{box-shadow:0 6px 18px rgba(37,99,235,.3)}
.btn-sm{padding:7px 12px;font-size:12px;border-radius:10px}

/* ── INPUTS ── */
.input,.sel{
  width:100%;background:var(--in-bg);color:var(--in-color);border:1px solid var(--in-border);
  border-radius:14px;padding:12px 13px;font-size:15px;outline:none;
  transition:border-color .15s,background .25s,color .25s;
}
.input:focus,.sel:focus{border-color:var(--accent);box-shadow:0 0 0 4px var(--accent-glow)}
.input::placeholder{color:var(--text-3)}
.sel{cursor:pointer}
.sel option{background:var(--bg-solid);color:var(--text)}

/* ── SLIDER ── */
.label{display:flex;justify-content:space-between;align-items:center;font-size:13px;color:var(--text-2);margin-bottom:6px}
.label strong{color:var(--text);font-variant-numeric:tabular-nums}
.slider{width:100%;height:36px;-webkit-appearance:none;background:transparent;cursor:pointer}
.slider::-webkit-slider-runnable-track{height:8px;background:var(--track);border-radius:999px;border:1px solid rgba(255,255,255,.15)}
.slider::-webkit-slider-thumb{-webkit-appearance:none;width:22px;height:22px;margin-top:-8px;border-radius:50%;background:#fff;border:2px solid var(--thumb-color);box-shadow:0 4px 12px rgba(0,0,0,.4)}
.slider::-moz-range-track{height:8px;background:var(--track);border-radius:999px}
.slider::-moz-range-thumb{width:22px;height:22px;border-radius:50%;background:#fff;border:2px solid var(--thumb-color)}
[data-theme="light"] .slider::-webkit-slider-runnable-track{border-color:rgba(0,0,0,.08)}
[data-theme="light"] .slider::-webkit-slider-thumb{box-shadow:0 2px 8px rgba(0,0,0,.2)}

/* ── CHIPS ── */
.chipRow{display:flex;flex-wrap:wrap;gap:6px}
.chip{
  font-size:12px;padding:6px 12px;border-radius:999px;cursor:pointer;
  background:var(--chip-bg);border:1px solid var(--chip-border);color:var(--chip-color);
  transition:background .15s,color .15s,border-color .15s;font-weight:600;
}
.chip.active,.chip:hover{background:var(--accent);border-color:var(--accent);color:#fff}

/* ── EMOJI PILLS ── */
.pillwrap{display:flex;flex-wrap:wrap;gap:8px}
.pill{
  font-size:24px;line-height:1;padding:9px 11px;border-radius:14px;cursor:pointer;user-select:none;
  background:var(--chip-bg);border:1px solid var(--chip-border);
  transition:transform .08s,background .15s,border-color .15s;
}
.pill[aria-pressed="true"]{
  background:var(--pill-on-bg);border-color:var(--pill-on-bdr);
  box-shadow:inset 0 0 0 1px rgba(167,139,250,.2),0 6px 16px rgba(0,0,0,.25);
  transform:translateY(-1px);
}

/* ── MODE TABS ── */
.mode-tabs{
  display:flex;gap:0;background:var(--bg-surface);border:1px solid var(--border);
  border-radius:16px;padding:4px;margin-bottom:14px;
}
.mode-tab{
  flex:1;padding:10px;border-radius:12px;border:0;font-size:14px;font-weight:800;cursor:pointer;
  background:transparent;color:var(--text-2);display:flex;align-items:center;justify-content:center;gap:7px;
  transition:background .18s,color .18s,box-shadow .18s;letter-spacing:.2px;
}
.mode-tab.active{background:var(--accent);color:#fff;box-shadow:0 4px 14px var(--accent-glow)}

/* ── MINIATURA SCALE DISPLAY ── */
.mini-scale{
  background:var(--chip-bg);border:1px solid var(--chip-border);
  border-radius:14px;padding:12px 14px;
  display:flex;align-items:center;justify-content:space-between;
  font-size:13px;color:var(--text-2);
}
.mini-scale strong{color:var(--accent);font-size:18px;font-weight:900}

/* ── RENDER TOGGLE (Suave / Pixelizado) ── */
.render-toggle{display:flex;gap:8px}
.render-btn{
  flex:1;padding:9px;border-radius:12px;border:1px solid var(--chip-border);
  background:var(--chip-bg);color:var(--chip-color);font-size:13px;font-weight:700;cursor:pointer;
  transition:background .15s,color .15s,border-color .15s;text-align:center;
}
.render-btn.active{background:var(--accent);border-color:var(--accent);color:#fff}

/* ── PREVIEW ── */
.preview{
  min-height:340px;display:grid;place-items:center;padding:14px;position:relative;
  border-radius:18px;overflow:hidden;
  background:radial-gradient(600px 200px at 50% -10%,rgba(139,92,246,.1),transparent);
  border:1px dashed var(--border);
}
[data-theme="light"] .preview{background:radial-gradient(600px 200px at 50% -10%,rgba(37,99,235,.06),transparent)}
.preview.dragover{border-color:var(--accent);box-shadow:0 0 0 4px var(--accent-glow) inset}
.ph{color:var(--text-3);text-align:center;padding:24px;font-size:14px}
.ph b{color:var(--text-2)}
.canvas-wrap{width:100%;display:grid;place-items:center}
canvas{
  max-width:100%;height:auto;border-radius:14px;
  background:var(--cvs-bg);
  box-shadow:0 12px 34px rgba(0,0,0,.4);
}
[data-theme="light"] canvas{box-shadow:0 4px 16px rgba(0,0,0,.12)}

.toolbar{display:flex;gap:10px;align-items:center;justify-content:space-between;margin-top:12px;flex-wrap:wrap}

/* ── COLOR SWATCH ── */
.colorSwatch{width:36px;height:36px;border-radius:10px;border:2px solid var(--btn-border);cursor:pointer;padding:0;background:none;flex-shrink:0}
.colorSwatch::-webkit-color-swatch-wrapper{padding:0}
.colorSwatch::-webkit-color-swatch{border-radius:8px;border:none}

/* ── TOGGLE SWITCH ── */
.toggle-row{display:flex;align-items:center;gap:8px;font-size:13px;color:var(--text-2);cursor:pointer;user-select:none}
.toggle-row input{width:38px;height:22px;-webkit-appearance:none;appearance:none;background:var(--chip-bg);border:1px solid var(--chip-border);border-radius:999px;position:relative;cursor:pointer;outline:none;transition:background .15s,border-color .15s}
.toggle-row input:checked{background:var(--accent);border-color:var(--accent)}
.toggle-row input::before{content:'';position:absolute;top:2px;left:2px;width:16px;height:16px;background:#fff;border-radius:50%;transition:transform .15s;box-shadow:0 2px 6px rgba(0,0,0,.3)}
.toggle-row input:checked::before{transform:translateX(16px)}

/* ── MINI INFO BOX ── */
.info-box{font-size:11.5px;color:var(--accent);background:rgba(41,182,246,.08);border:1px solid rgba(41,182,246,.2);border-radius:10px;padding:8px 10px;line-height:1.5}
[data-theme="light"] .info-box{background:rgba(37,99,235,.06);border-color:rgba(37,99,235,.2)}

.kbd{font-family:ui-monospace,Menlo,monospace;background:var(--chip-bg);border:1px solid var(--chip-border);padding:2px 6px;border-radius:6px;font-size:11px;color:var(--text-2)}
.footer{margin-top:20px;text-align:center;color:var(--text-3);font-size:12px}

/* ── HIDDEN UTIL ── */
.hidden{display:none!important}
</style>
</head>
<body>
<div class="bg"></div><div class="glow"></div>
<div class="wrap">

  <!-- HEADER -->
  <header>
    <div class="brand">
      <div class="logo">DH</div>
      <div>
        <h1>Detilli Hub</h1>
        <div class="sub">Foto → Emoji</div>
      </div>
    </div>
    <!-- TEMA -->
    <div class="theme-toggle">
      <button class="theme-btn" data-theme="light">☀️ Claro</button>
      <button class="theme-btn" data-theme="dark">🌙 Escuro</button>
      <button class="theme-btn" data-theme="system">💻 Sistema</button>
    </div>
  </header>

  <main>
    <!-- ═══ COLUNA ESQUERDA ═══ -->
    <section>

      <!-- UPLOAD -->
      <div class="card pad">
        <h2>Imagem</h2>
        <div class="stack">
          <div class="row">
            <input id="file" type="file" accept="image/*" hidden>
            <button id="btnUpload" class="btn" style="flex:1">📁 Enviar arquivo</button>
            <button id="btnPaste" class="btn" title="Colar (Ctrl+V)">📋 Colar</button>
          </div>
          <div class="row">
            <input id="urlIn" class="input" placeholder="URL da imagem (https://...)" inputmode="url" autocomplete="off">
            <button id="btnLoad" class="btn">Carregar</button>
          </div>
          <div class="note">Arraste e solte no preview ou use <span class="kbd">Ctrl</span>+<span class="kbd">V</span>.</div>
        </div>
      </div>

      <!-- SELETOR DE MODO -->
      <div class="mode-tabs">
        <button class="mode-tab active" data-mode="emoji">🎭 Modo Emoji</button>
        <button class="mode-tab" data-mode="miniatura">🖼️ Miniatura Fiel</button>
      </div>

      <!-- ═══ PAINEL: MODO EMOJI ═══ -->
      <div id="panelEmoji">

        <div class="card pad">
          <h2>Miniaturização</h2>
          <div class="stack">
            <div>
              <div class="label"><span>Colunas de emoji</span><strong id="sizeVal">60</strong></div>
              <input id="size" class="slider" type="range" min="20" max="140" step="2" value="60">
              <div class="chipRow" style="margin-top:6px">
                <div class="chip" data-val="35">Baixo</div>
                <div class="chip active" data-val="60">Médio</div>
                <div class="chip" data-val="90">Alto</div>
                <div class="chip" data-val="130">Extremo</div>
              </div>
            </div>
            <div>
              <div class="label"><span>Tamanho do emoji (px)</span><strong id="tileVal">20</strong></div>
              <input id="tileSize" class="slider" type="range" min="10" max="36" step="1" value="20">
            </div>
          </div>
        </div>

        <div class="card pad">
          <h2>Emojis</h2>
          <div class="stack">
            <select id="emojiSet" class="sel">
              <option value="classico">Clássico</option>
              <option value="rostos">Rostos</option>
              <option value="natureza">Natureza</option>
              <option value="comida">Comida</option>
              <option value="animais">Animais</option>
              <option value="cores">Blocos de Cor (alta fidelidade)</option>
            </select>
            <div class="row">
              <span class="note" style="flex:1">toque para ativar/desativar</span>
              <button id="btnSelAll" class="btn btn-sm">Todos</button>
              <button id="btnSelNone" class="btn btn-sm">Nenhum</button>
            </div>
            <div id="emojis" class="pillwrap"></div>
            <div class="sep" style="margin:6px 0"></div>
            <div class="row">
              <input id="customEmoji" class="input" placeholder="Emojis personalizados (ex: 🚀 🎧)">
              <button id="btnAddEmoji" class="btn">+ Add</button>
            </div>
          </div>
        </div>

      </div><!-- /panelEmoji -->

      <!-- ═══ PAINEL: MINIATURA FIEL ═══ -->
      <div id="panelMini" class="hidden">

        <div class="card pad">
          <h2>Redução da Imagem</h2>
          <div class="stack">
            <div class="info-box">
              Reduz a imagem a uma miniatura real em pixels. O slider vai de <b>20% de um polegar</b> (muito pequeno) até <b>tamanho polegar</b> (miniatura nítida).
            </div>
            <div>
              <div class="label"><span>Nível de miniaturização</span><strong id="miniPctLabel">50%</strong></div>
              <input id="miniSlider" class="slider" type="range" min="5" max="100" step="1" value="50">
              <div class="chipRow" style="margin-top:6px">
                <div class="chip" data-mini="5">Micro (5%)</div>
                <div class="chip" data-mini="20">20% polegar</div>
                <div class="chip" data-mini="50">50%</div>
                <div class="chip" data-mini="100">Polegar</div>
              </div>
            </div>
            <div class="mini-scale" id="miniScale">
              <span>Resolução de saída</span>
              <strong id="miniDimLabel">—</strong>
            </div>
            <div>
              <div class="label" style="margin-bottom:8px"><span>Renderização</span></div>
              <div class="render-toggle">
                <button class="render-btn active" data-render="smooth">🔵 Suave</button>
                <button class="render-btn" data-render="pixel">🟣 Pixelizado</button>
              </div>
            </div>
            <label class="toggle-row">
              <input type="checkbox" id="miniZoom" checked>
              <span>Exibir ampliada para visualizar detalhe</span>
            </label>
          </div>
        </div>

      </div><!-- /panelMini -->

      <!-- ═══ AJUSTES DE IMAGEM (compartilhado) ═══ -->
      <div class="card pad">
        <h2>Ajustes de Imagem</h2>
        <div class="stack">
          <div>
            <div class="label"><span>Brilho</span><strong id="brightVal">100%</strong></div>
            <input id="brightness" class="slider" type="range" min="40" max="180" step="2" value="100">
          </div>
          <div>
            <div class="label"><span>Contraste</span><strong id="contrastVal">100%</strong></div>
            <input id="contrast" class="slider" type="range" min="40" max="200" step="2" value="100">
          </div>
          <div>
            <div class="label"><span>Saturação</span><strong id="satVal">100%</strong></div>
            <input id="saturation" class="slider" type="range" min="0" max="200" step="2" value="100">
          </div>
          <div>
            <div class="label" style="margin-bottom:6px"><span>Filtro</span></div>
            <select id="filterSel" class="sel">
              <option value="normal">Normal</option>
              <option value="pb">Preto e branco</option>
              <option value="sepia">Sépia</option>
              <option value="negativo">Negativo</option>
            </select>
          </div>
          <div>
            <div class="label" style="margin-bottom:6px"><span>Fundo do mosaico</span></div>
            <div class="row">
              <select id="bgSel" class="sel" style="flex:1">
                <option value="transparent">Transparente</option>
                <option value="black">Preto</option>
                <option value="white">Branco</option>
                <option value="custom">Personalizado</option>
              </select>
              <input type="color" id="bgColor" class="colorSwatch" value="#0a0f1f">
            </div>
          </div>
          <label class="toggle-row">
            <input type="checkbox" id="autoToggle" checked>
            <span>Atualizar automaticamente</span>
          </label>
        </div>
      </div>

      <button id="btnConvert" class="btn btn-primary" style="width:100%;padding:15px 16px;font-size:16px">✨ Converter</button>
    </section>

    <!-- ═══ COLUNA DIREITA: PREVIEW ═══ -->
    <section class="card pad">
      <h2>Prévia</h2>
      <div id="drop" class="preview">
        <div id="ph" class="ph">Solte uma imagem aqui<br><b>JPG, PNG, WEBP, GIF</b></div>
        <div id="cw" class="canvas-wrap" hidden>
          <canvas id="out" width="800" height="600"></canvas>
        </div>
      </div>
      <div class="toolbar">
        <div class="note" id="infoTxt">Nenhuma imagem carregada</div>
        <div class="row wrap">
          <button id="btnClear" class="btn btn-sm">Limpar</button>
          <button id="btnCopy" class="btn btn-sm" disabled>📋 Copiar</button>
          <button id="btnJpg" class="btn btn-sm" disabled>⬇️ JPG</button>
          <button id="btnPng" class="btn btn-sm" disabled>⬇️ PNG</button>
        </div>
      </div>
    </section>
  </main>

  <div class="footer">Feito com ♥ no Detilli Hub — tudo roda no seu navegador, sem servidores.</div>
</div>

<script>
(() => {
  const $ = s => document.querySelector(s);

  // ═══════════════════════════════════════
  //  1. TEMA — Claro / Escuro / Sistema
  // ═══════════════════════════════════════
  const THEME_KEY = 'dh-theme';
  const mq = window.matchMedia('(prefers-color-scheme: dark)');

  function applyTheme(pref) {
    const dark = pref === 'dark' || (pref === 'system' && mq.matches);
    document.documentElement.setAttribute('data-theme', dark ? 'dark' : 'light');
    document.querySelectorAll('.theme-btn').forEach(b =>
      b.classList.toggle('active', b.dataset.theme === pref)
    );
  }

  function setTheme(pref) {
    localStorage.setItem(THEME_KEY, pref);
    applyTheme(pref);
  }

  (function initTheme() {
    const saved = localStorage.getItem(THEME_KEY) || 'dark';
    applyTheme(saved);
    document.querySelectorAll('.theme-btn').forEach(b =>
      b.classList.toggle('active', b.dataset.theme === saved)
    );
  })();

  document.querySelectorAll('.theme-btn').forEach(b =>
    b.addEventListener('click', () => setTheme(b.dataset.theme))
  );
  mq.addEventListener('change', () => {
    if (localStorage.getItem(THEME_KEY) === 'system') applyTheme('system');
  });

  // ═══════════════════════════════════════
  //  2. ALTERNÂNCIA DE MODO
  // ═══════════════════════════════════════
  let currentMode = 'emoji';
  const panelEmoji = $('#panelEmoji');
  const panelMini  = $('#panelMini');

  document.querySelectorAll('.mode-tab').forEach(tab => {
    tab.addEventListener('click', () => {
      currentMode = tab.dataset.mode;
      document.querySelectorAll('.mode-tab').forEach(t =>
        t.classList.toggle('active', t === tab)
      );
      panelEmoji.classList.toggle('hidden', currentMode !== 'emoji');
      panelMini.classList.toggle('hidden',  currentMode !== 'miniatura');
      scheduleAutoConvert();
    });
  });

  // ═══════════════════════════════════════
  //  3. EMOJI PALETTE
  // ═══════════════════════════════════════
  const EMOJI_SETS = {
    classico: ['😀','❤️','⭐','🎮','📻','🔥','💧','🌙','☀️','🍀','🍕','🎈'],
    rostos:   ['😀','😁','😂','😍','😎','🥳','😢','😡','🤔','😴','😱','🥰'],
    natureza: ['🌳','🌸','☀️','🌊','🍂','⛰️','🌵','🌈','❄️','🍁','🌻','🌴'],
    comida:   ['🍎','🍌','🍇','🥑','🍕','🍫','🍉','🍓','🥕','🍩','🍊','🍋'],
    animais:  ['🐶','🐱','🦊','🐼','🐸','🐵','🦁','🐨','🐷','🐧','🐢','🦋'],
    cores:    ['🟥','🟧','🟨','🟩','🟦','🟪','⬛','⬜','🟫','🔴','🟡','🟢'],
  };

  let presetList  = [...EMOJI_SETS.classico];
  let customList  = [];
  let selected    = new Set(presetList);

  function activeList() {
    const merged = [...presetList];
    customList.forEach(e => { if (!merged.includes(e)) merged.push(e); });
    return merged;
  }

  // Cor média real por emoji (renderiza + amostra)
  const colorCache = new Map();
  const probe = document.createElement('canvas');
  probe.width = probe.height = 32;
  const pctx = probe.getContext('2d', { willReadFrequently: true });

  function emojiColor(emoji) {
    if (colorCache.has(emoji)) return colorCache.get(emoji);
    pctx.clearRect(0, 0, 32, 32);
    pctx.textAlign = 'center'; pctx.textBaseline = 'middle';
    pctx.font = '28px "Apple Color Emoji","Segoe UI Emoji","Noto Color Emoji",system-ui,sans-serif';
    try { pctx.fillText(emoji, 16, 17); } catch(e) {}
    let r=0,g=0,b=0,n=0;
    try {
      const d = pctx.getImageData(0,0,32,32).data;
      for (let i=0;i<d.length;i+=4) {
        const a=d[i+3]; if(a<20) continue;
        r+=d[i]*a; g+=d[i+1]*a; b+=d[i+2]*a; n+=a;
      }
    } catch(e) {}
    const col = n===0 ? [140,140,140] : [r/n|0, g/n|0, b/n|0];
    colorCache.set(emoji, col);
    return col;
  }

  function splitEmojis(str) {
    str = (str||'').trim(); if (!str) return [];
    if (typeof Intl !== 'undefined' && Intl.Segmenter) {
      try {
        return [...new Intl.Segmenter('en',{granularity:'grapheme'}).segment(str)]
          .map(s=>s.segment).filter(s=>s.trim());
      } catch(e) {}
    }
    return [...str].filter(s=>s.trim());
  }

  function renderPills() {
    const wrap = $('#emojis');
    wrap.innerHTML = '';
    activeList().forEach(e => {
      const b = document.createElement('button');
      b.type='button'; b.className='pill'; b.textContent=e;
      b.setAttribute('aria-pressed', selected.has(e)?'true':'false');
      b.onclick = () => {
        if (selected.has(e)) { if (selected.size>1) selected.delete(e); }
        else selected.add(e);
        b.setAttribute('aria-pressed', selected.has(e)?'true':'false');
        scheduleAutoConvert();
      };
      wrap.appendChild(b);
    });
  }

  $('#emojiSet').onchange = () => {
    presetList = [...EMOJI_SETS[$('#emojiSet').value]];
    selected = new Set(activeList());
    renderPills(); scheduleAutoConvert();
  };
  $('#btnSelAll').onclick  = () => { selected=new Set(activeList()); renderPills(); scheduleAutoConvert(); };
  $('#btnSelNone').onclick = () => {
    const l=activeList(); selected=new Set(l.length?[l[0]]:[]);
    renderPills(); scheduleAutoConvert();
  };
  $('#btnAddEmoji').onclick = () => {
    splitEmojis($('#customEmoji').value).forEach(e=>{
      if (!customList.includes(e)) customList.push(e);
      selected.add(e);
    });
    $('#customEmoji').value='';
    renderPills(); scheduleAutoConvert();
  };
  $('#customEmoji').addEventListener('keydown', e=>{ if(e.key==='Enter') $('#btnAddEmoji').click(); });
  renderPills();

  // ═══════════════════════════════════════
  //  4. CARREGAMENTO DE IMAGEM
  // ═══════════════════════════════════════
  let img = new Image(); img.crossOrigin='anonymous';
  let hasImage = false;
  const out = $('#out'), ph=$('#ph'), cw=$('#cw'), drop=$('#drop');
  const infoTxt = $('#infoTxt');
  const btnPng=$('#btnPng'), btnJpg=$('#btnJpg'), btnCopy=$('#btnCopy');

  function setInfo(t){ infoTxt.textContent=t; }

  function loadFile(f){ setImage(URL.createObjectURL(f), f.name||'Arquivo'); }

  function setImage(src, label='Imagem') {
    hasImage=false; btnPng.disabled=btnJpg.disabled=btnCopy.disabled=true;
    img=new Image(); img.crossOrigin='anonymous';
    img.onload = () => {
      hasImage=true; ph.hidden=true; cw.hidden=false;
      showOriginal();
      setInfo(`${label} • ${img.naturalWidth}×${img.naturalHeight}`);
      scheduleAutoConvert(true);
    };
    img.onerror = () => {
      alert('Não foi possível carregar. Se for URL, tente enviar o arquivo.');
      setInfo('Erro ao carregar');
    };
    img.src=src;
  }

  function showOriginal() {
    try {
      const scale=Math.min(1,1100/img.naturalWidth);
      const w=Math.max(1,img.naturalWidth*scale|0), h=Math.max(1,img.naturalHeight*scale|0);
      out.width=w; out.height=h;
      out.style.imageRendering='auto';
      const ctx=out.getContext('2d');
      ctx.clearRect(0,0,w,h); ctx.drawImage(img,0,0,w,h);
    } catch(e) {}
  }

  // Upload controls
  $('#btnUpload').onclick = () => $('#file').click();
  $('#file').onchange = e => { const f=e.target.files?.[0]; if(f) loadFile(f); e.target.value=''; };
  $('#btnLoad').onclick = () => { const v=$('#urlIn').value.trim(); if(v) setImage(v,'URL'); };
  $('#btnPaste').onclick = async () => {
    try {
      const items = await navigator.clipboard.read();
      for(const it of items) for(const t of it.types)
        if(t.startsWith('image/')){ loadFile(new File([await it.getType(t)],'colado.png',{type:t})); return; }
      alert('Nenhuma imagem. Use Ctrl+V diretamente na página.');
    } catch { alert('Pressione Ctrl+V na página para colar.'); }
  };
  document.addEventListener('paste', e => {
    for(const it of e.clipboardData?.items||[])
      if(it.type?.startsWith('image/')){ const f=it.getAsFile(); if(f){ e.preventDefault(); loadFile(f); break; } }
  });
  ['dragenter','dragover'].forEach(ev=>drop.addEventListener(ev,e=>{ e.preventDefault(); e.stopPropagation(); drop.classList.add('dragover'); }));
  ['dragleave','drop'].forEach(ev=>drop.addEventListener(ev,e=>{ e.preventDefault(); e.stopPropagation(); drop.classList.remove('dragover'); }));
  drop.addEventListener('drop', e=>{ const f=e.dataTransfer?.files?.[0]; if(f?.type.startsWith('image/')) loadFile(f); });

  // ═══════════════════════════════════════
  //  5. CONTROLES: LABELS + CHIPS
  // ═══════════════════════════════════════
  $('#size').oninput = () => { $('#sizeVal').textContent=$('#size').value; scheduleAutoConvert(); };
  $('#tileSize').oninput = () => { $('#tileVal').textContent=$('#tileSize').value; scheduleAutoConvert(); };
  $('#brightness').oninput = () => { $('#brightVal').textContent=$('#brightness').value+'%'; scheduleAutoConvert(); };
  $('#contrast').oninput   = () => { $('#contrastVal').textContent=$('#contrast').value+'%'; scheduleAutoConvert(); };
  $('#saturation').oninput = () => { $('#satVal').textContent=$('#saturation').value+'%'; scheduleAutoConvert(); };
  $('#filterSel').onchange = () => scheduleAutoConvert();
  $('#bgSel').onchange = () => { $('#bgColor').style.display=$('#bgSel').value==='custom'?'block':'none'; scheduleAutoConvert(); };
  $('#bgColor').oninput = () => scheduleAutoConvert();
  $('#bgColor').style.display = 'none';

  // Chips de preset (modo emoji)
  document.querySelectorAll('.chip[data-val]').forEach(c => {
    c.addEventListener('click', () => {
      $('#size').value=c.dataset.val;
      $('#sizeVal').textContent=c.dataset.val;
      document.querySelectorAll('.chip[data-val]').forEach(x=>x.classList.remove('active'));
      c.classList.add('active');
      scheduleAutoConvert();
    });
  });

  // ═══════════════════════════════════════
  //  6. CONTROLES: MINIATURA FIEL
  // ═══════════════════════════════════════
  let renderMode = 'smooth';

  function updateMiniLabel() {
    const pct = parseInt($('#miniSlider').value, 10);
    $('#miniPctLabel').textContent = pct + '%';
    const aspect = img.naturalWidth && img.naturalHeight ? img.naturalWidth/img.naturalHeight : 1.5;
    const w = Math.max(20, Math.round(pct/100*200));
    const h = Math.max(10, Math.round(w/aspect));
    $('#miniDimLabel').textContent = `${w} × ${h} px`;
  }

  $('#miniSlider').oninput = () => { updateMiniLabel(); scheduleAutoConvert(); };

  document.querySelectorAll('.chip[data-mini]').forEach(c => {
    c.addEventListener('click', () => {
      $('#miniSlider').value = c.dataset.mini;
      updateMiniLabel();
      document.querySelectorAll('.chip[data-mini]').forEach(x=>x.classList.remove('active'));
      c.classList.add('active');
      scheduleAutoConvert();
    });
  });

  document.querySelectorAll('.render-btn').forEach(b => {
    b.addEventListener('click', () => {
      renderMode = b.dataset.render;
      document.querySelectorAll('.render-btn').forEach(x=>x.classList.remove('active'));
      b.classList.add('active');
      scheduleAutoConvert();
    });
  });

  $('#miniZoom').onchange = () => scheduleAutoConvert();

  // ═══════════════════════════════════════
  //  7. FILTRO
  // ═══════════════════════════════════════
  function buildFilter() {
    let f = `brightness(${$('#brightness').value}%) contrast(${$('#contrast').value}%) saturate(${$('#saturation').value}%)`;
    const fi = $('#filterSel').value;
    if (fi==='pb')       f+=' grayscale(100%)';
    if (fi==='sepia')    f+=' sepia(100%)';
    if (fi==='negativo') f+=' invert(100%)';
    return f;
  }

  function resolveBg() {
    const v=$('#bgSel').value;
    if(v==='transparent') return null;
    if(v==='black')       return '#000';
    if(v==='white')       return '#fff';
    return $('#bgColor').value;
  }

  // ═══════════════════════════════════════
  //  8. CONVERSÃO: MODO EMOJI
  // ═══════════════════════════════════════
  function convertEmoji() {
    const activePal = activeList().filter(e=>selected.has(e));
    if (!activePal.length) { setInfo('Selecione pelo menos um emoji.'); return; }

    const cols = parseInt($('#size').value, 10);
    const aspect = img.naturalWidth / img.naturalHeight;
    const rows = Math.max(10, Math.round(cols / aspect));

    const samp = document.createElement('canvas');
    samp.width=cols; samp.height=rows;
    const sctx = samp.getContext('2d',{willReadFrequently:true});
    sctx.imageSmoothingEnabled=true; sctx.imageSmoothingQuality='high';
    try { sctx.filter=buildFilter(); sctx.drawImage(img,0,0,cols,rows); }
    catch(e) { alert('Imagem com CORS. Baixe e envie o arquivo.'); return; }
    const data = sctx.getImageData(0,0,cols,rows).data;

    const palette = activePal.map(e=>({c:e, col:emojiColor(e)}));

    const maxDim = 2200;
    let tile = parseInt($('#tileSize').value, 10);
    if(cols*tile>maxDim) tile=Math.max(6,maxDim/cols|0);
    if(rows*tile>maxDim) tile=Math.max(6,Math.min(tile,maxDim/rows|0));

    out.width=cols*tile; out.height=rows*tile;
    out.style.imageRendering='auto';
    const ctx=out.getContext('2d');
    ctx.clearRect(0,0,out.width,out.height);
    const bg=resolveBg();
    if(bg){ ctx.fillStyle=bg; ctx.fillRect(0,0,out.width,out.height); }
    ctx.textAlign='center'; ctx.textBaseline='middle';
    ctx.font=`${Math.floor(tile*.84)}px "Apple Color Emoji","Segoe UI Emoji","Noto Color Emoji",system-ui,sans-serif`;

    for(let y=0;y<rows;y++) {
      const row=y*cols;
      for(let x=0;x<cols;x++) {
        const i=(row+x)*4;
        const a=data[i+3]; if(a<10) continue;
        const r=data[i],g=data[i+1],b=data[i+2];
        let best=palette[0].c, bestD=Infinity;
        for(const p of palette) {
          const dr=(r-p.col[0])*.30, dg=(g-p.col[1])*.59, db=(b-p.col[2])*.11;
          const d=dr*dr+dg*dg+db*db;
          if(d<bestD){ bestD=d; best=p.c; }
        }
        ctx.fillText(best, x*tile+tile/2, y*tile+tile/2+.5);
      }
    }

    setInfo(`Mosaico ${cols}×${rows} • saída ${out.width}×${out.height}px`);
    ph.hidden=true; cw.hidden=false;
    btnPng.disabled=false; btnJpg.disabled=false;
    btnCopy.disabled=!navigator.clipboard||!window.ClipboardItem;
  }

  // ═══════════════════════════════════════
  //  9. CONVERSÃO: MINIATURA FIEL
  //     Módulo separado — não toca na lógica de emoji
  // ═══════════════════════════════════════
  function convertMiniatura() {
    const pct    = parseInt($('#miniSlider').value, 10); // 5-100
    const aspect = img.naturalWidth / img.naturalHeight;
    const targetW = Math.max(20, Math.round(pct / 100 * 200));
    const targetH = Math.max(10, Math.round(targetW / aspect));

    // Renderiza na resolução miniaturizada com filtros aplicados
    const samp = document.createElement('canvas');
    samp.width=targetW; samp.height=targetH;
    const sctx = samp.getContext('2d',{willReadFrequently:true});
    sctx.imageSmoothingEnabled = true;
    sctx.imageSmoothingQuality = 'high';
    try {
      sctx.filter = buildFilter();
      sctx.drawImage(img, 0, 0, targetW, targetH);
    } catch(e) { alert('Imagem com CORS. Baixe e envie o arquivo.'); return; }

    const bg = resolveBg();

    // Canvas de saída no tamanho nativo da miniatura
    out.width=targetW; out.height=targetH;
    const ctx = out.getContext('2d');
    ctx.clearRect(0,0,targetW,targetH);
    if(bg){ ctx.fillStyle=bg; ctx.fillRect(0,0,targetW,targetH); }
    ctx.drawImage(samp, 0, 0);

    // Renderização CSS: suave ou pixelizada
    out.style.imageRendering = renderMode==='pixel' ? 'pixelated' : 'auto';

    ph.hidden=true; cw.hidden=false;
    setInfo(`Miniatura fiel • ${targetW}×${targetH}px (${pct}% do polegar)${renderMode==='pixel'?' • Pixelizado':' • Suave'}`);
    btnPng.disabled=false; btnJpg.disabled=false;
    btnCopy.disabled=!navigator.clipboard||!window.ClipboardItem;
  }

  // ═══════════════════════════════════════
  //  10. DISPATCHER + AUTO-CONVERT
  // ═══════════════════════════════════════
  function convert() {
    if (!hasImage) return;
    if (currentMode==='emoji') convertEmoji();
    else convertMiniatura();
  }

  let autoTimer=null;
  function scheduleAutoConvert(immed) {
    if (!$('#autoToggle').checked || !hasImage) return;
    clearTimeout(autoTimer);
    autoTimer = setTimeout(convert, immed?0:220);
  }

  $('#btnConvert').onclick = () => {
    if (!hasImage){ alert('Envie uma imagem primeiro.'); return; }
    convert();
    if(window.innerWidth<920) out.scrollIntoView({behavior:'smooth',block:'start'});
  };

  // ═══════════════════════════════════════
  //  11. EXPORTAÇÃO
  // ═══════════════════════════════════════
  function exportCanvas(type, quality, filename) {
    try {
      // Para JPG: garante fundo branco se canvas tem transparência
      let src = out;
      if (type==='image/jpeg' && resolveBg()===null) {
        src=document.createElement('canvas');
        src.width=out.width; src.height=out.height;
        const c=src.getContext('2d');
        c.fillStyle='#ffffff'; c.fillRect(0,0,src.width,src.height);
        c.drawImage(out,0,0);
      }
      src.toBlob(blob=>{
        if(!blob){ alert('Falha ao exportar.'); return; }
        const a=document.createElement('a');
        a.href=URL.createObjectURL(blob); a.download=filename;
        document.body.appendChild(a); a.click(); a.remove();
        setTimeout(()=>URL.revokeObjectURL(a.href),1000);
      }, type, quality);
    } catch(e){ alert('Erro ao exportar. Use arquivo local (sem CORS).'); }
  }

  $('#btnPng').onclick = () => exportCanvas('image/png', 1, 'detilli-hub.png');
  $('#btnJpg').onclick = () => exportCanvas('image/jpeg', 0.92, 'detilli-hub.jpg');
  $('#btnCopy').onclick = async () => {
    try {
      out.toBlob(async blob=>{
        if(!blob){ alert('Falha ao copiar.'); return; }
        await navigator.clipboard.write([new ClipboardItem({'image/png':blob})]);
        setInfo('✅ Copiado para a área de transferência!');
        setTimeout(()=>setInfo(infoTxt.textContent.replace('✅ Copiado para a área de transferência!','')),2200);
      },'image/png');
    } catch(e){ alert('Copiar não disponível neste navegador. Use o botão PNG.'); }
  };

  $('#btnClear').onclick = () => {
    hasImage=false; img=new Image(); img.crossOrigin='anonymous';
    out.getContext('2d').clearRect(0,0,out.width,out.height);
    ph.hidden=false; cw.hidden=true;
    setInfo('Nenhuma imagem carregada');
    btnPng.disabled=btnJpg.disabled=btnCopy.disabled=true;
    $('#urlIn').value=''; $('#file').value='';
  };

  // ═══════════════════════════════════════
  //  12. INIT
  // ═══════════════════════════════════════
  $('#sizeVal').textContent  = $('#size').value;
  $('#tileVal').textContent  = $('#tileSize').value;
  $('#brightVal').textContent= $('#brightness').value+'%';
  $('#contrastVal').textContent=$('#contrast').value+'%';
  $('#satVal').textContent   = $('#saturation').value+'%';
  updateMiniLabel();
})();
</script>
</body>
</html>
